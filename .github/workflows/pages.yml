name: Deploy web app to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# only one deployment at a time; newer pushes cancel in-progress ones
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Install Shiki (coverage tooling)
        run: npm install --prefix scripts/shiki

      - name: Check coverage badge is up to date
        run: |
          uv run python scripts/shiki_coverage.py --badge assets/shiki-coverage.svg
          git diff --exit-code assets/shiki-coverage.svg

      - name: Run tests
        run: uv run pytest -q

      - name: Build web bundle (web/data)
        run: uv run python scripts/build_web.py

      - uses: actions/configure-pages@v6

      - uses: actions/upload-pages-artifact@v5
        with:
          path: web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
